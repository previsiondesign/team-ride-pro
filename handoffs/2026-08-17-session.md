# Session Handoff — 2026-08-17

Big non-code session: **rebrand decision + email/DNS migration off Google Workspace/Wix.** Almost nothing here lives in the repo — this handoff is the record.

## The rebrand: TeamRide Pro → PreRide, under Berm Labs LLC

- **Berm Labs LLC** formed; bermlabs.com already in M365 with working mail.
- **"TeamRide Pro" is not defensible**: teamride.com is a live carpool product *for sports teams/practices* (same customer); TEAMride (KATNIK4, LLC, Sacramento cycling studio) asserts the mark in its ToS with iOS/Android apps; "Ride Pro" separately occupied. "Pro" carries ~no trademark weight, so TRP ≈ TeamRide vs. TeamRide.
- **Screened ~45 names in two passes** (trail vocab, then race-ops vocab after user course-correct: product is NICA race rosters + practice ops, not recreational rides).
  - **Winner: PreRide** — no product/app/mark found; NICA-native (pre-riding race courses); suggestive-not-descriptive = registrable sweet spot.
  - Runner-up: CallUps (race-staging vocab, but CallUp Connect + CALLUP.net crowd the software space).
  - Killed with cause: **StartList** (startlist.app is nearly this exact product), RideRoster ("Carpool Planner: Ride Roster" on App Store), HeroDirt (MTB apparel LLC), PitBoard (NICA's own platform is "Pit Zone"), TeamTent/TrailCrew/SquadList/Paceline etc.
- **Domains registered at Porkbun 2026-08-17**: `preridemtb.com` (primary marketing/email; ".com + mtb" reads right to NICA parents) + `preride.app` (future canonical app URL). Deliberately skipped: prerideteam.com (reshuffles "teamridepro" spoken aloud), preridemtb.app (no traffic path).
- **preride.com**: parked at GoDaddy, ask $5K+. Plan: counteroffer $1.2–1.5K, walk if refused. **Decision pending.**
- **Attorney trademark clearance on PreRide still needed** before filing or marketing spend (screening was informal). Quick self-check: tmsearch.uspto.gov → "preride" → LIVE filter.

## Email/DNS migration — executed and verified today

Verified registrar first: **teamridepro.com is registered at Porkbun, not Wix** → cancelling Wix cannot forfeit the domain; no transfer-out needed.

- Both new zones added to the existing Cloudflare account (gabe/love NS, same as teamridepro/bermlabs). Porkbun parking records (A 207.207.210.x, wildcard/www CNAME→uixie.porkbun.com, fwd1/fwd2 MX, \_acme TXTs, parking SPF) **purged** from preridemtb.com.
- **M365**: preridemtb.com + teamridepro.com both added and verified (teamridepro via the M365↔Cloudflare connect flow — added only the MS TXT, didn't touch other records). Wizard rules held: "I'll add DNS myself" + Exchange-records-only.
- **DNS cutover, verified from public DNS afterward**:
  - preridemtb.com: MX 0 `preridemtb-com.mail.protection.outlook.com`, autodiscover CNAME→autodiscover.outlook.com (grey), SPF `v=spf1 include:spf.protection.outlook.com include:amazonses.com ~all`, DMARC `p=none; rua=mailto:adam@previsiondesign.com; fo=1`.
  - teamridepro.com: 5 Google MX **deleted**; M365 MX + autodiscover **added** (Option A catch-net); **the long-standing double-SPF PermError fixed** (two conflicting root SPF records → one, Cloudflare's own dashboard had been flagging it); DMARC updated with rua.
  - Preserved & re-verified: apex/www CNAME→previsiondesign.github.io (site + /tamhighmtb/ return 200), `resend._domainkey` DKIM, `send` MX/TXT (Resend). Resend API confirms teamridepro.com still **verified/sending-enabled**.
- **Aliases**: adam@preridemtb.com + adam@teamridepro.com on the main mailbox (info@ variants also exist, unticked in send-from list). Shared-mailbox plan: hello@/support@preridemtb.com.
- **Inbound tested end-to-end**: sent via Resend API from noreply@teamridepro.com → both aliases → Resend status **delivered** to M365. Headers spf=pass dkim=pass.
- **Send-from-alias**: EAC org toggle was already on. New aliases still rewrite outbound From to adam@previsiondesign.com; A/B test with adam@bermlabs.com **works correctly** → conclusion: directory-replication lag on hour-old aliases, not misconfiguration. **Retest ~24h (8/18).**
- **Google Takeout** (Mail/Contacts/Calendar, .zip 2GB parts) started 8/17 ~3:58 PM. Download from takeout.google.com directly (don't rely on notification email — MX now points at M365). Links expire 7 days; limited download attempts. **Open and spot-check the .mbox before any cancellation.** .mbox reads in Thunderbird.

## Runbook artifact (the durable plan)

`https://claude.ai/code/artifact/7a1687d9-016b-46e8-acc0-220112bee13a` — Rev C ("PreRide-First"). Phases 0–8, BIND import files used for the cutover, rollback card (Google MX set — valid only while Workspace subscription is alive), naming-decision appendix with the full screening table.

## Remaining

- **8/18**: retest From: adam@preridemtb.com → external recipient shows correctly.
- **This week**: download + verify Takeout export.
- **~8/24 (after quiet soak)**: Phase 6 — cancel Google Workspace, cancel Wix plan, delete stale `google-site-verification` TXT. Ensure no service uses @teamridepro.com as login/recovery first.
- **No deadline (Phases 7–8)**: GitHub org `bermlabs` + repo rename → `preride` (fold together so URLs break once; re-create CRON_SECRET/SUPABASE_FUNCTION_URL secrets, update www CNAME, re-add Pages custom domain); Supabase/Resend/Render ownership under Berm Labs; verify preridemtb.com in Resend + flip the four edge functions off `onboarding@resend.dev` to `noreply@preridemtb.com` via FROM_EMAIL env; privacy-policy entity/name; product rebrand strings/manifests; preride.com counteroffer; M365 DKIM selectors for the new domains (optional hardening).

## Still outstanding from May (unchanged)

- `.github/workflows/attendance-poll.yml` rewrite (off-season skip + 5xx retry) sits **uncommitted** — local OAuth lacks `workflow` scope. `gh auth refresh -s workflow` then commit/push.
- `next/index.html` redesign prototype (mockup-C, single collapsible planner sidebar) — uncommitted WIP, keep.
- Supabase publishable-vs-legacy-JWT key mismatch; ADD_CONFIRMATION_TRACKING / ALTER_SLACK_POLL_RESPONSES_3STATE not pushed; color group names bug.
